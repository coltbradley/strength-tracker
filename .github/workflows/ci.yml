name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  database:
    name: migrations + views + RLS (PGlite)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/build-exercise-seed.mjs
      - run: npm --prefix scripts install
      - run: node scripts/validate-db.mjs

  mcp-server:
    name: edge function typecheck (deno)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x
      - run: deno check index.ts
        working-directory: supabase/functions/mcp-server

  pwa:
    name: pwa build + tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: pwa/package-lock.json
      - run: npm ci
        working-directory: pwa
      - run: npm run build
        working-directory: pwa
      - run: npm test -- --run
        working-directory: pwa
